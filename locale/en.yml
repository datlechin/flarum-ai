datlechin-ai:
  admin:
    settings:
      provider_label: Provider
      provider_help: Anthropic supports text generation only. OpenAI and Google also offer embeddings and moderation.

      api_key_label: API key
      api_key_help: Required to call {provider}.

      base_url_label: Base URL
      base_url_help: Leave empty to use the official endpoint. Override only when routing through a proxy or compatible gateway.
      base_url_placeholder: https://api.example.com

      text_model_label: Text model
      text_model_help: Used for completions and streaming.

      embeddings_model_label: Embeddings model
      embeddings_model_help: Used to produce vector representations.

      moderation_model_label: Moderation model
      moderation_model_help: Used to classify content for safety scores.

      custom_model_option: Custom
      custom_model_placeholder: e.g. gpt-4o-2024-08-06
